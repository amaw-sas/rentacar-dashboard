# Migración productiva del ETL legacy → prod — 2026-06-01 (#23)

**Issue:** #23 · **Destino:** producción `ilhdholjrnbycyvejsub` · **Resultado:** completado y verificado.

## Veredicto

Las reservas del sistema legacy están en producción. El ETL corrió contra prod por el transaction
pooler (6543), insertó **12,446 reservas**, rechazó 521 dentro de la taxonomía conocida, reconcilió
a nivel de fila y pasó todas las verificaciones de integridad. Las 455 reservas vivas del dashboard
quedaron intactas. Los customers ya estaban en prod desde #19 (10,744), así que esta corrida fue
reservations-only con un pase idempotente de customers que no insertó nada nuevo.

## Cómo se corrió

Launcher `scripts/migration/run-prod-migration.sh --commit --snapshot-confirmed`, con ref-guard
contra `ilhdholjrnbycyvejsub` y el transaction pooler en el puerto 6543. Antes del commit se tomó un
snapshot de las 455 reservas vivas (`pg_dump`/COPY de `public.reservations`, guardado fuera del repo
por PII — no se commitea). Secuencia: preflight (exit 0) → customers commit → reservations commit →
`verify-prod-run.sql`.

## Resultados por entidad

### Customers — pase idempotente (ya migrados en #19)

| Métrica | Valor |
|---|---|
| Insertados | 0 |
| Skipped — already_migrated | 10,744 |
| Skipped — conflict_existing | 30 |
| Rejected | 0 |
| Reconcilia | sí (0 + 12,846 + 121 placeholder = 12,967) |

Re-correr el ETL de customers no insertó nada: confirma la idempotencia que ya se había validado en #19.

### Reservations — el trabajo de #23

| Métrica | Valor |
|---|---|
| Filas legacy escaneadas | 12,967 |
| **Insertadas** | **12,446 (95.98 %)** |
| Skipped | 0 |
| Rejected | 521 (4.02 %) |
| Tiempo | 1.7 s |

**Taxonomía de los 521 rechazos** (todos esperados):

| Motivo | Conteo |
|---|---|
| `pickup_location_null` | 354 |
| `customer_not_migrated` | 120 |
| `return_location_null` | 47 |

**Reconciliación:** 12,446 insertadas + 0 skipped + 521 rejected = 12,967. Cierra.

Frente a la firma de producto (que aprobó un 4.03 % de pérdida sobre el dry-run de #22): la corrida
real perdió **4.02 %** — una reserva más migró porque prod tiene clientes preexistentes del dashboard
que el branch vacío de #22 no tenía. Dentro de lo firmado.

## Verificación de integridad (independiente, vía MCP — distinta al launcher)

| Check | Resultado |
|---|---|
| Reservations migradas | 12,446 |
| `count(distinct _legacy_id)` | 12,446 → **sin duplicados** |
| `count(distinct _legacy_migrated_at)` | 1 → un solo run, rollback scope limpio |
| Total reservations | 12,901 (= 455 vivas + 12,446 migradas) |
| Reservas vivas preexistentes | 455 → **intactas** (marker NULL), idénticas al snapshot |
| FK huérfano a customer | 0 |
| Reservas migradas con ubicación nula | 0 |

`verify-prod-run.sql` (corrido por el launcher) dio las 4 aserciones en PASS.

**Distribuciones de dominio** (suman 12,446):

- **booking_type:** standard 11,131 · standard_with_insurance 1,192 · monthly 123
- **franchise:** alquilame 7,228 · alquicarros 2,624 · alquilatucarro 2,594
- **status:** utilizado 3,503 · no_contactado 3,290 · cancelado 2,501 · no_recogido 1,825 · sin_disponibilidad 682 · reservado 531 · baneado 109 · indeterminado 5

## Nota registrada — 5 dedup-escape de #19

El ETL emitió un warning: 5 identificaciones de customer colapsan a una clave normalizada existente
(rasgo preexistente de los datos de #19, last-writer-wins en el lookup). Esas reservas quedan atadas a
un customer válido con esa identificación normalizada. No es algo que introduzca #23 — estaba idéntico
en el dry-run de #22, ya aceptado. Se deja documentado para trazabilidad.

## Rollback disponible

Si hiciera falta revertir: `docs/data-ops/2026-05-28-issue-20-etl-reservations/rollback.sql` borra solo
`reservations WHERE _legacy_migrated_at IS NOT NULL` (las 12,446, un solo timestamp de run), con guard
de FK a commissions; `notification_logs` cae por CASCADE. Las 455 reservas vivas no se tocan. El
snapshot CSV pre-corrida es la red de respaldo adicional. **No** correr el rollback de customers (#19).

## Próximo

- Firma de producto de la aceptación del resultado.
- #24 (cleanup): drop de los markers (`_legacy_id`, `_legacy_migrated_at` vía migraciones 049/051) y
  archivado de logs — solo tras la firma de aceptación.

## Procedencia

Reportes por fila con PII (IDs rechazados) en `docs/migration-runs/etl-{customers,reservations}-2026-06-01T21-*.jsonl` — **gitignored**, no se commitean. Snapshot de reservas vivas fuera del repo. Este documento es solo agregados por taxonomía.
